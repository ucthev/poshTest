- name: get os name (windows)
  hosts: winServers
  gather_facts: false
  tasks:
    - name: run powershell module to get os name
      script: get_osname.yml
      register: powershell
    - debug: var=powershell
